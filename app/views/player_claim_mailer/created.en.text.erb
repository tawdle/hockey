User <%= @player_claim.creator.name %> has requested ownership of the player <%= @player_claim.player.name_and_number %> on your team <%= @player.team.name %>.

The email address provided by this user is <%= @player_claim.creator.email %>. You should approve this request only if this email address matches your records.

If the address does not match your records, this request may be an error, and you should deny the request.

To view the request and approve or deny it, cut and paste the following link into your browser's address bar:

<%= player_claim_url(@player, @player_claim) %>

