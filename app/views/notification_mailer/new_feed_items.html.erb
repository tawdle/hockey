<!DOCTYPE html>

<html>
  <head>
    <title>BigShot.io</title>
    <meta http-equiv="content-type" content="text/html;charset=utf-8">
  </head>

  <body style="margin:0;padding:0;">

  <table><tbody>
    <tr><td>
      <%= link_to(image_tag(attachments['logo.png'].url,  :alt => "BigShot Logo", :width => 100), root_url) %>
    </td></tr>
    <tr><td>
      <p><%= t("navigation.greeting") %>, <%= @user.name %>!</p>
      <p><%= t(".what_is_new_html", :link => link_to("BigShot.io", root_url, :style => "text-decoration:none;")) %></p>
    </td></tr>
  </tbody></table>

    <table>
      <tbody>
        <% @items.each do |item| %>
          <%= render :partial => "feed_item", :locals => { :item => item } %>
        <% end %>
      </tbody>
    </table>

    <p><%= t(".to_see_or_reply_html", :link => link_to("BigShot.io", root_url, :style => "text-decoration:none;")) %></p>
    <% link = link_to(t(".unsubscribe_link_text"), {:only_path => false, :controller => :unsubscribe, :action => :unsubscribe, :user_id => @user.id, :token => @user.unsubscribe_token, :category => :daily_activity_feed}, :style => "text-decoration:none;") %>
    <p><%= t(".unsubscribe_html", :link => link) %></p>
  </body>
</html>

